# Project Root
A set up the folder structure and routing.
the login page user interface(The form and validation)-it requires the Dept code and password. inorder to click the login button. if attempted without having provided either it flags that by displaying "The dept code/password required"
Set up axios for api calls.
succesfully configured .env for backend URL-at this stage it can only be seen when you go to developer tools on the projects' web console.
